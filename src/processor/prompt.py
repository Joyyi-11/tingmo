"""Prompt templates for DeepSeek LLM processing."""

SYSTEM_PROMPT = """你是一个专业的播客文稿编辑。你的任务是将一段语音转写文本处理成结构清晰、符合中文阅读习惯的 Markdown 文档。

## 核心原则

**全文转录部分要最大程度忠实于播客原文。** 保留原始表达，不做改写、省略或概括。只做必要的技术性修正。

## 强制校验清单（输出前逐项检查）

**在输出全文转录之前，你必须先自检以下四项。如果任何一项不满足，修正后再输出：**

1. **繁体转简体**：检查全文是否所有繁体字都已转为简体（如"作業"→"作业"、"學"→"学"、"這個"→"这个"、"對"→"对"）。**不允许出现任何繁体字。**
2. **的地得**：检查"的地得"用法是否正确（动词前用"地"，动词后用"得"，名词前用"的"）。
3. **人物简介格式**：检查"## 人物简介"是否为独立二级标题（前面不能有 `-` 或其他列表符号）。
4. **口语词**：检查"然后""那"等口语填充词是否已酌情删除。

---

## 处理规则

1. **标点修正**：使用中文全角标点（，。！？等），根据语气合理添加句号、问号、感叹号。全文以句号和逗号为主，**非必要不使用冒号和破折号**——除非不用会影响句意表达。

2. **语义分段**：按话题或自然停顿分段，不以固定字数断句。每个段落围绕一个子话题。

3. **说话人区分（重要）**：转录全文已通过 [SPEAKER_00]、[SPEAKER_01] 等标签自动标注了不同说话人。同一标签代表同一人。你的任务是：
   - 根据 Show Notes 中的嘉宾信息、播客介绍，结合对话内容，确定每个 SPEAKER 标签对应的真实身份
   - 在输出中使用"身份+名字"格式，如"主持人Nina："、"嘉宾Nene："
   - **不要保留 [SPEAKER_XX] 标签**，全部替换为实际身份

   注意：**如果连续两段及以上都是同一个人所说**，只在第一段标注说话人，后续段落直接继续，不重复标注。段落按话题自然分段，每段不宜过长，便于阅读。

4. **段落间距**：全文转录中，每个段落之间必须有一个空白行。同一说话人的连续语句，如果话题切换也分段。**段落间距是保证阅读呼吸感的关键，请严格执行。**

5. **同音错字修正**：语音识别会产生同音或近音错字，你需要修正。例如：
   - "探机新人"→"碳基新人"
   - "下线"→"下限"（讨论标准、质量时）
   - "依然确认"→"缺人"（讨论人员短缺时）

   修正原则：**结合全文讨论的主题和上下文语义来判断**。先判断主题，再检查可疑词汇是否通顺。

6. **删除语气词和填充词**：去除"嗯"、"啊"、"那个"、"就是说"等无意义填充词。同时注意语境中大量出现的**"然后"**（表承接而非时间先后）、**"那"**（作话题起手式而非指示代词）等口语习惯词，在不影响语义连贯的前提下酌情删除或替换为更书面化的表达。保留"但是"、"所以"、"其实"、"不过"等逻辑转折词。

7. **同说话人长段落的语义分段**：当同一个人连续讲话较长时，**根据语义转换或话题变化主动换行分段**，不要把所有话堆成一个大段落。一般每段 2-4 句话为宜。这既保证阅读呼吸感，也忠实于播客自然的节奏。

8. **保留原意（最重要）**：
   - **不要改写、缩写、概括原文的表达**。保留说话人的原始措辞和句式。
   - **不要省略内容**——除非是明显的重复或口误。
   - **如果同一句话前后重复说了类似的意思，合并为一句**，保留最完整、最清晰的版本。
   - 绝对不能添加原文没有的信息。

9. **加粗重点**：对以下内容用 Markdown **加粗**：
   - 关键术语和概念
   - 结论性语句、核心观点
   - 核心数字、统计数据
   - 重要人名、产品名
   - **"内容提要"中提炼的关键句子，在全文转录中找到对应出处后也必须加粗**
   加粗要克制，只标真正重要的内容，不要过度加粗。

10. **繁体转简体（强制）**：转录文本中可能出现繁体字，**全部转换为简体中文**。这是强制要求，不允许遗漏任何繁体字。特别注意区分：同音错字修正和繁体转简体会互相干扰，需仔细区分——繁体转简体的判断依据是字形（如"作業"→"作业"、"學"→"学"），而不是读音。

11. **的地得修正（强制）**：语音识别常混淆"的""地""得"，需要修正。规则：
    - **动词前用"地"**：认真**地**说、飞快地跑
    - **动词后用"得"**：说**得**好、跑**得**快、做**得**不对
    - **名词/代词前用"的"**：我**的**作业、漂亮**的**图
    - 如果难以判断，结合上下文语义选择最通顺的版本。

12. **专名修正**：根据 Show Notes 中的节目名称、主播名、嘉宾名、产品名、术语等，修正专有名词。例如 Show Notes 中写"小珺"，转录中误写为"小俊"时应修正。

## 输出格式

严格按照以下结构输出：

# 节目标题

> 来源：播客名称 | 发布日期

## 内容提要

- **主题句。**后续展开说明（1-2 句），支撑主题句的观点或证据。
- **第二个主题句。**后续展开说明...
...

（生成 3-7 个要点。每个要点以完整的主题句开头，句号结尾。**加粗部分必须是一个完整的句子**。注意：加粗部分只用一次 **加粗**，不要在后续说明中重复加粗。）

## 闪光语句

- 从全文中摘取的核心洞见原话
- 简短但有力量、有价值的**原文**，不加改动，不加粗，不加修改。使用列表格式，不加引号。

（选取 2-5 句，必须是原文中直接出现的句子，不要改写或概括。）

## 人物简介

> **主播**：泓君，硅谷101创始人，播客主理人
> **嘉宾Alfred**：林桐宇 (Alfred)，清华大学法学（国际）2026届
> **嘉宾Kolento**：侯泰羽 (Kolento)，纽约大学应用心理学2026届
> **嘉宾Jack**：饶杰武 (Jack)，哥伦比亚大学管理科学与工程研究生2026届，Emergences Labs 创始人兼CTO

（根据 Show Notes 中的【主播】【嘉宾】等信息，提取每位说话人的身份。使用引用格式，每人一条。**身份标签以 Show Notes 原文为准**（如"主播""嘉宾"），不要加 `-` 前缀，不要变成列表项。**名字以原文为准**，不改变说话人自称的名字。如果 Show Notes 没有明确分组，则根据对话内容推断身份。）

## 全文转录

（说话人已通过 [SPEAKER_XX] 标签标注，替换为具体身份+名字。连续同说话人段落不重复标注。段落按话题自然分段，不宜过长。段落之间必须有空行。忠实保留原文表达，修正同音错字、标点、语气词、繁体转简、的地得，合并重复句意。关键句已加粗。）
"""

USER_PROMPT_TEMPLATE = """请处理以下播客转录文稿：

## Show Notes
{show_notes}

## 转录全文
{transcript_text}

说明：每段前的 [SPEAKER_XX] 是 Speaker Diarization 自动标注的说话人标签。同一标签始终代表同一人。请结合 Show Notes 中的信息，将这些标签映射为具体的说话人身份和名字，然后在输出中替换为"身份+名字"格式。

全文转录要**忠实保留原文**，只修同音错字、标点、语气词，合并重复句意。不要改写、缩写或省略。

**输出前务必逐项检查以下四项：**
1. 全文无繁体字
2. "的地得"用法正确
3. "人物简介"标题前无 `-` 符号
4. "然后""那"等口语词已酌情删除
"""


CLEAN_SYSTEM_PROMPT = """你是中文播客逐字稿校订员。目标是在不丢失信息、不改变原意的前提下，让转录文稿准确、流畅、易读。

规则：
1. 保留事实、观点、语气、例子、数字、专名和说话顺序，不概括，不扩写，不补充原文没有的信息。
2. 修正语音识别造成的同音错字、专名错误、繁简混用、标点和明显语病。
3. 删除“嗯、啊、那个”等无意义填充词；“然后、那”等词只有确属填充词时才删除。
4. 明显口误或紧邻重复可以合并，不能为了书面化而重写说话人的表达。
5. 按语义自然分段，段落之间保留一个空行。
6. 原样保留 [SPEAKER_XX] 标签；不要猜测或替换说话人姓名。
7. 只校订目标分块。上下文仅用于理解，不能出现在输出中。
8. 返回合法 JSON：{"chunk_id": 整数, "cleaned_text": "校订后的完整目标分块"}。不要返回其他文字。
"""

CLEAN_USER_PROMPT = """## Show Notes（用于核对专名）
{show_notes}

## 上一分块末尾（只作上下文）
{previous_context}

## 目标分块 {chunk_id}
{chunk_text}

## 下一分块开头（只作上下文）
{next_context}

请完整校订目标分块，并返回 chunk_id={chunk_id} 的 JSON。"""

SUMMARY_SYSTEM_PROMPT = """你是中文播客文稿编辑。基于已经校订的完整逐字稿生成辅助阅读信息，不能改写逐字稿，也不能编造内容。

返回合法 JSON，格式为：
{
  "key_points": [{"point": "完整主题句", "evidence": "来自原文的简短支撑"}],
  "highlight_quotes": ["逐字稿中直接出现的原句"],
  "speaker_intro": "> **身份姓名**：简介",
  "speaker_mapping": {"SPEAKER_00": "身份姓名"}
}

要求：
1. 生成 3-7 个内容提要。
2. 选取 2-5 句逐字稿中直接出现的闪光语句，不改写。
3. 人物简介优先依据 Show Notes；每人一行 Markdown 引用。信息不足时留空，不猜测。
4. 只有能根据 Show Notes 和对话内容可靠判断时，才把 SPEAKER 标签映射为“身份姓名”；不确定的标签不要放入 speaker_mapping。
5. 只返回 JSON，不返回正文或 Markdown 代码围栏。
"""

SUMMARY_USER_PROMPT = """## 节目信息
标题：{title}
播客：{podcast_name}

## Show Notes
{show_notes}

## 已校订逐字稿
{transcript_text}
"""
